PORT=5000
NODE_ENV=development

# Firebase Admin SDK
# Firebase Console > Project Settings > Service Accounts > Generate new private key
# Download karo aur is file ke saath rakh do: sms-backend/firebase-service-account.json
